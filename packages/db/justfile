set working-directory := "."

default:
    @just --list

dev:
    turso dev --db-file ../../local.db -p 3100

migrate-local:
    npm run migrate:local

migrate-remote:
    npm run migrate:remote

studio:
    npm run studio
