set working-directory := "."

default:
    @just --list

dev:
    turso dev --db-file ../../local.db -p 3100

migrate-local:
    deno run migrate:local

migrate-remote:
    deno run migrate:remote

studio:
    deno run studio
