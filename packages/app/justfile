set working-directory := "."

default:
    @just --list

dev:
    gleam dev

test:
    just ../db/seed
    gleam test

build:
    deno run compile:css
    deno run compile:client
    gleam build
    gleam export erlang-shipment
